import { Router } from "express";
import UserAuthController from "../controllers/UserAuthController.js";

const router = Router();

// router.post("/register", UserAuthController.register); // Example route for user registration
export default router;
